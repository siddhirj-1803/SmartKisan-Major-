# Web server
flask
flask-cors
werkzeug
Flask-SQLAlchemy

# Machine Learning
# Choose a TensorFlow version compatible with your environment. The default CPU build is fine for inference.
tensorflow
numpy
pillow
pandas

# LLM Integration
python-dotenv
langchain-google-genai
pillow

# Production server (optional)
gunicorn
